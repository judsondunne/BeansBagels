import React from 'react';
import { View, Text } from 'react-native';

const Profile = () => {
    return (
        <View>
            <Text style={{fontSize: 40, fontWeight: '500', marginTop: 50}}>JMAR smells</Text>
        </View>
    );
};

export default Profile;